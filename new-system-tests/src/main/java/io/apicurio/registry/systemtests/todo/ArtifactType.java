package io.apicurio.registry.systemtests.todo;

// TODO Generate this from schema
public enum ArtifactType {
    AVRO,
    PROTOBUF,
    JSON,
    OPENAPI,
    ASYNCAPI,
    GRAPHQL,
    KCONNECT,
    WSDL,
    XSD,
    XML
}
